/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.html'],
    theme: {
        extend: {
            fontFamily: {
                'parkisans': ['Parkisans', 'sans-serif'],
            }
        }
    },
    plugins: []
};
